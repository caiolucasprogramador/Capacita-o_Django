# Capacitação_Django
Capacitação para membros da CEOS JR sobre o framework Django

## 1. Configurando o ambiente
O Django protege o sistema operacional, por isso usamos ambientes virtuais.

```bash
# Criar a pasta do projeto
mkdir meu_projeto && cd meu_projeto

# Criar e ativar o ambiente virtual
python3 -m venv venv
source venv/bin/activate
